@model PagedList.Core.IPagedList<CK_CDO_Final.Entities.Hose>

@{
    ViewData["Title"] = "HOSE";
}

<div class="row">
    <div class="col-md-12">
        <h3 class="title-5 m-b-35">Danh sách giá chứng khoán sàn HOSE</h3>
        <div class="table-data__tool">
            <div class="table-data__tool-left">
                <form asp-action="Index" method="get">
                    <div class="rs-select2--light rs-select2--sm">
                        <input type="text" class="form-control" placeholder="Search for" id="searchString" name="searchString" value="@ViewData["Search"]" />
                    </div>
                    <div class="rs-select2--light rs-select2--sm">
                        <input type="date" id="date" name="date" class="form-control" value="@ViewData["Date"]" />
                    </div>
                    <button class="au-btn-filter" type="submit">
                        <i class="fa fa-search"></i>
                    </button>
                </form>

            </div>
            <div class="table-data__tool-right">
                <a class="au-btn au-btn-icon au-btn--green au-btn--small" asp-action="Create">
                    Add item
                </a>
            </div>
        </div>
        <!-- DATA TABLE -->
        <div class="table-responsive table-responsive-data2">
            <table class="table table-data2">
                <thead>
                    <tr class="row">
                        <th class="col-lg-1">
                            <a asp-action="Index" asp-route-date="@ViewData["Date"]" asp-route-searchString="@ViewData["Search"]" asp-route-sortOrder="@ViewData["Ma"]">@Html.DisplayNameFor(model => model.First().MA)</a>
                        </th>
                        <th class="col-lg-2">
                            @Html.DisplayNameFor(model => model.First().NGAY)
                        </th>
                        <th class="col-lg-2">
                            <a asp-action="Index" asp-route-date="@ViewData["Date"]" asp-route-searchString="@ViewData["Search"]" asp-route-sortOrder="@ViewData["Open"]">@Html.DisplayNameFor(model => model.First().GIAMOCUA)</a>
                        </th>
                        <th class="col-lg-1">
                            @Html.DisplayNameFor(model => model.First().GIATRAN)
                        </th>
                        <th class="col-lg-1">
                            @Html.DisplayNameFor(model => model.First().GIASAN)
                        </th>
                        <th class="col-lg-2">
                            <a asp-action="Index" asp-route-date="@ViewData["Date"]" asp-route-searchString="@ViewData["Search"]" asp-route-sortOrder="@ViewData["Close"]">@Html.DisplayNameFor(model => model.First().GIADONGCUA)</a>
                        </th>
                        <th class="col-lg-1">
                            @Html.DisplayNameFor(model => model.First().KHOILUONG)
                        </th>
                        <th class="col-lg-2"></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr class="tr-shadow row">
                            <td class="col-lg-1">
                                <a asp-controller="CompanyDetails" asp-action="Details" asp-route-id="@item.MA">@Html.DisplayFor(modelItem => item.MA)</a>
                            </td>
                            <td class="col-lg-2">
                                @Html.DisplayFor(modelItem => item.NGAY)
                            </td>
                            <td class="col-lg-2">
                                @Html.DisplayFor(modelItem => item.GIAMOCUA)
                            </td>
                            <td class="col-lg-1">
                                @Html.DisplayFor(modelItem => item.GIATRAN)
                            </td>
                            <td class="col-lg-1">
                                @Html.DisplayFor(modelItem => item.GIASAN)
                            </td>
                            <td class="col-lg-2">
                                @Html.DisplayFor(modelItem => item.GIADONGCUA)
                            </td>
                            <td class="col-lg-1">
                                @Html.DisplayFor(modelItem => item.KHOILUONG)
                            </td>
                            <td class="col-lg-2">
                                <div class="table-data-feature">
                                    <a class="item" asp-action="Edit" asp-route-id="@item.ID" data-toggle="tooltip" data-placement="top" title="Edit">
                                        <i class="zmdi zmdi-edit"></i>
                                    </a>
                                    <a class="item" asp-action="Delete" asp-route-id="@item.ID" data-toggle="tooltip" data-placement="top" title="Delete">
                                        <i class="zmdi zmdi-delete"></i>
                                    </a>
                                    <a class="item" asp-action="Details" asp-route-id="@item.ID" data-toggle="tooltip" data-placement="top" title="More">
                                        <i class="zmdi zmdi-more"></i>
                                    </a>
                                </div>
                            </td>
                        </tr>
                        <tr class="spacer"></tr>
                    }
                </tbody>
            </table>
            <pager class="pager-container" list="@Model" asp-action="Index" asp-controller="Hoses" />
        </div>
        <!-- END DATA TABLE -->
    </div>
</div>
